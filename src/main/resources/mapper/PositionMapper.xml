<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.example.zixuan.mapper.PositionMapper">

    <select id="findById" resultType="org.example.zixuan.model.Position">
        SELECT p.*, u.name AS teacher_name,
               (SELECT COUNT(*) FROM applications a
                WHERE a.position_id = p.id AND a.status = 'approved') AS current_count
        FROM positions p
        JOIN users u ON p.teacher_id = u.id
        WHERE p.id = #{id}
    </select>

    <select id="findOpen" resultType="org.example.zixuan.model.Position">
        SELECT p.*, u.name AS teacher_name,
               (SELECT COUNT(*) FROM applications a
                WHERE a.position_id = p.id AND a.status = 'approved') AS current_count
        FROM positions p
        JOIN users u ON p.teacher_id = u.id
        WHERE p.status = 'open'
        ORDER BY p.created_at DESC
    </select>

    <select id="findByTeacher" resultType="org.example.zixuan.model.Position">
        SELECT p.*, u.name AS teacher_name,
               (SELECT COUNT(*) FROM applications a
                WHERE a.position_id = p.id AND a.status = 'approved') AS current_count
        FROM positions p
        JOIN users u ON p.teacher_id = u.id
        WHERE p.teacher_id = #{teacherId}
        ORDER BY p.created_at DESC
    </select>

    <select id="findAll" resultType="org.example.zixuan.model.Position">
        SELECT p.*, u.name AS teacher_name,
               (SELECT COUNT(*) FROM applications a
                WHERE a.position_id = p.id AND a.status = 'approved') AS current_count
        FROM positions p
        JOIN users u ON p.teacher_id = u.id
        ORDER BY p.created_at DESC
    </select>

</mapper>
